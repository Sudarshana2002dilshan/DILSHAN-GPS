body { margin: 0; font-family: sans-serif; background: #0a192f; color: #fff; height: 100vh; overflow-x: hidden; }
.tab-pane { display: none; height: 100%; padding-bottom: 70px; }
.active { display: block; }
.logo-container { text-align: center; padding: 15px; }
.main-logo { width: 80px; height: 80px; border-radius: 50%; border: 2px solid #64ffda; }
.card { background: #112240; margin: 10px; padding: 15px; border-radius: 15px; text-align: center; box-shadow: 0 5px 10px rgba(0,0,0,0.3); }
.huge-data { font-size: 35px; font-weight: 900; color: #64ffda; }
.huge-bold { font-size: 28px; font-weight: 900; color: #fff; }
.time-data { font-size: 25px; font-weight: 800; color: #fbc02d; margin-top: 5px; }
.label { font-size: 12px; color: #8892b0; text-transform: uppercase; margin-bottom: 5px; }
.bottom-nav { position: fixed; bottom: 0; width: 100%; display: flex; background: #0a192f; border-top: 1px solid #233554; }
.bottom-nav button { flex: 1; padding: 15px; border: none; background: none; color: #8892b0; font-weight: bold; }
iframe { width: 100%; height: 100%; border: none; }
.btn { width: 100%; padding: 12px; background: #004d40; color: white; border-radius: 10px; border: none; }
.btn-wa { display: block; padding: 12px; background: #25D366; color: white; text-decoration: none; border-radius: 10px; margin-top: 10px; }
