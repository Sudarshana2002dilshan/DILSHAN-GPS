:root { 
    --bg: #0a192f; 
    --card: #112240; 
    --text: #ffffff; 
    --accent: #64ffda; 
}
.light-mode { 
    --bg: #f4f4f4; 
    --card: #ffffff; 
    --text: #000000; 
}
body { 
    background: var(--bg); 
    color: var(--text); 
    font-family: sans-serif; 
    height: 100vh; 
    margin:0; 
}
.tab-pane { 
    display: none; 
    height: 85vh; 
    overflow-y: auto; 
}
.tab-pane.active { 
    display: block; 
}
@keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.app-title {
    text-align: center; 
    color: #64ffda; 
    margin-top: -10px; 
    margin-bottom: 20px;
    font-weight: 900;
}

/* ⚠️ Hazard Alert Bar */
.whitebar {
    background: #ff1744;
    color: #ffffff;
    font-weight: 900;
    text-align: center;
    padding: 20px;
    font-size: 26px; 
    box-shadow: 0px 5px 15px rgba(0,0,0,0.5);
    border-bottom: 3px solid #fff;
    text-transform: uppercase;
    animation: blink-bg 1s infinite alternate; 
}
@keyframes blink-bg {
    from { background: #ff1744; }
    to { background: #b71c1c; }
}

.animate-card { animation: slideUp 0.5s ease-out; }
@keyframes slideUp { from { opacity:0; transform:translateY(30px); } to { opacity:1; transform:translateY(0); } }

.card { 
    background: var(--card); 
    margin: 15px; 
    padding: 25px; 
    border-radius: 25px; 
    text-align: center; 
    border: 2px solid var(--accent); 
}
.label {
    font-weight: 800;
    color: #ff9800;
    font-size: 16px;
    margin-bottom: 5px;
}

/* GPS Coordinates text size & color */
.huge-text { 
    font-size: 48px; 
    font-weight: 900; 
    color: #ffffff; 
    text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
}
.light-mode .huge-text {
    color: #000000 !important; 
}

.time-text { font-size:28px; color:#fbc02d; margin-top:10px; font-weight: bold; }
.speed-text { font-size: 52px; font-weight: 900; color: #ff5722; }
.wind-text { font-weight: 800; font-size: 28px; color: #1CFF13; margin-top:10px; }
.wind-dir { font-weight: 800; font-size: 28px; color: #1CFF13; margin-top:10px; }

/* 🌊 Ocean Current Styles */
.current-text { 
    font-weight: 800; 
    font-size: 32px; 
    color: #00e676; 
    margin-top: 5px; 
}
.current-dir { 
    font-weight: 800; 
    font-size: 24px; 
    color: #ffffff; 
    margin-top: 5px; 
}
.light-mode .current-dir {
    color: #000000 !important;
}

.bottom-nav { position: fixed; bottom: 0; width: 100%; display: flex; background: #004d40; z-index: 100;}
.bottom-nav button { flex: 1; padding: 20px; border:none; background:none; color: white; font-size: 24px; cursor: pointer; }
.input-field { width: 90%; padding: 12px; margin: 10px 0; border-radius: 10px; border: 2px solid #ccc; font-weight: bold;}
.btn { width: 95%; padding: 15px; margin-top:10px; border-radius: 10px; border: none; background: #00796b; color: white; font-weight: 900; font-size: 16px; cursor: pointer;}

/* 🌙 Night Vision Mode එකට අදාළ CSS */
.night-vision-mode {
    --bg: #000000 !important;
    --card: #120000 !important;
    --text: #ff1744 !important;
    --accent: #b71c1c !important;
}

.night-vision-mode body,
.night-vision-mode .card,
.night-vision-mode h2,
.night-vision-mode h3,
.night-vision-mode h4,
.night-vision-mode .label,
.night-vision-mode .huge-text,
.night-vision-mode .speed-text,
.night-vision-mode .wind-text,
.night-vision-mode .wind-dir,
.night-vision-mode .current-text,
.night-vision-mode .current-dir,
.night-vision-mode .time-text {
    background-color: #000000 !important;
    color: #ff1744 !important;
    border-color: #ff1744 !important;
    text-shadow: none !important;
}

.night-vision-mode .btn {
    background: #b71c1c !important;
    color: #ffffff !important;
}

.night-vision-mode .bottom-nav {
    background: #1a0000 !important;
}

